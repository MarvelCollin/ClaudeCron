{
  "name": "claude-cron",
  "version": "1.0.0",
  "description": "Command-line Claude automation for scheduled runs.",
  "main": "src/cli.js",
  "scripts": {
    "start": "node src/cli.js run",
    "login": "node src/cli.js login",
    "check": "node --check src/cli.js && node --check src/automation.js"
  },
  "dependencies": {
    "playwright-core": "^1.53.0"
  }
}
