{
  "name": "claude-cron",
  "version": "1.0.0",
  "description": "Electron scheduler for Claude automation in Brave.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js && node --check src/automation.js && node --check src/scheduler.js && node --check src/store.js"
  },
  "dependencies": {
    "playwright-core": "^1.53.0"
  },
  "devDependencies": {
    "electron": "^42.4.0"
  }
}
